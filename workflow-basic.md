# Workflow Basic

```
$ git clone
$ git branch -a
$ git checkout -b topic-branch
$ // Hacking / rubah code
$ git add .
$ git commit -m "commit log"
$ git checkout master
$ git pull 
$ git merge topic-branch
$ //Resolve conflicts 
$ git push
```
